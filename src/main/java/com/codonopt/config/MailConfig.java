package com.codonopt.config;

import org.springframework.context.annotation.Configuration;

/**
 * 邮件配置
 */
@Configuration
public class MailConfig {
    // Spring Boot自动配置会处理邮件设置
    // 这里保留用于未来的自定义配置
}
